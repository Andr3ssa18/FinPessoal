<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor quando o item está selecionado -->
    <item android:color="@color/primaryYellow" android:state_checked="true" />
    <!-- Cor padrão para quando não está selecionado -->
    <item android:color="@color/text_secondary" android:state_checked="false"/>
</selector>
    